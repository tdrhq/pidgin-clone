AC_INIT(pidgin-userguide, 2.5.1devel, devel@pidgin.im)
AM_INIT_AUTOMAKE([foreign dist-bzip2])

dnl #make sure we have xsltproc
AC_PATH_PROG(XSLTPROC, [xsltproc])

AC_OUTPUT([
	Makefile
	images/Makefile
	xml/Makefile
	xml/custom.xsl
])

